[diff]
	compactionHeuristic = true
	algorithm = patience
[user]
	name = Kir Shatrov
	email = shatrov@me.com
[alias]
	st = status
	ci = commit -m
	br = branch
	co = checkout
	df = diff --color
	dif = diff --cached
	lg = log -p
	pr = pull --rebase
  prom = pull origin master --rebase
  pick = cherry-pick
[color]
	ui = true
[core]
	pager = less -r
	autocrlf = input
	excludesfile = ~/.gitignore_global
	editor = /usr/local/bin/nvim
[push]
	default = simple
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
[github]
	user = kirs
[protocol]
	version = 2
